# Normalize line endings so the repo is consistent regardless of who clones it.
# This is a Windows-only WPF solution, so CRLF is the natural default for sources.
* text=auto eol=crlf

# Treat known-binary assets as binary (no EOL munging, no diffing).
*.png binary
*.jpg binary
*.ico binary
*.db binary
